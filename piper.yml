version:
  class: 'piper.version.StaticVersion'
  version: '0.0.1'

envs:
  local:
    version: '0.0.1'
    class: 'piper.env.Env'

steps:
  test:
    version: '0.0.2'
    class: 'piper.step.Step'
    command: 'tox'
  build:
    version: '0.0.2'
    class: 'piper.step.Step'
    command: 'python setup.py sdist'
    depends: 'test'

jobs:
  test:
  - 'test'
  build:
  - 'build'
